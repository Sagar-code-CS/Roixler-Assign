import { Bar, Pie } from 'react-chartjs-2';

const BarChart = ({ data }) => (
    <Bar data={data} options={{ }} />
);

const PieChart = ({ data }) => (
    <Pie data={data} options={{ }} />
);
